# Demo: mid-project (fictional)
A tiny repo so the Verification boards have something to audit. Run `run verification`.
The seeded ledger shows the kind of findings the boards produce (incl. an intentional P0 to find).
