# Cambium Institute - developer / code-quality tooling
# Install:  pip install -r requirements-dev.txt -c constraints.txt
#
# These are the tools the quality-CI workflow (.github/workflows/quality.yml)
# and the local pre-commit hooks (.pre-commit-config.yaml) run. Runtime deps
# live in requirements.txt; test-runner pins live in constraints.txt. Keeping
# the dev tools here means CONTRIBUTORS get the same linters CI uses with one
# install, and CI installs the exact same set.

# Test runner + coverage (the GATING OS-matrix job and the coverage job)
pytest>=7.0
pytest-asyncio>=0.21
pytest-cov>=5.0
coverage[toml]>=7.0
httpx>=0.27
pyyaml>=6.0

# Linters / formatters / analyzers
ruff>=0.6            # GATING lint (config in pyproject.toml [tool.ruff])
black>=24.0         # ADVISORY format check (never auto-applied in CI)
mypy>=1.10          # ADVISORY type check over tools/ (config in [tool.mypy])

# Security / supply-chain (both ADVISORY)
bandit>=1.7         # static security scan of tools/ and web/
pip-audit>=2.7      # known-CVE audit of declared dependencies

# Local hook runner (not required in CI; see docs/reference/CODE_QUALITY.md)
pre-commit>=3.7
