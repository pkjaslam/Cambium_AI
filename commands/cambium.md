---
description: Run a task the Cambium way — Orchestrator + the needed councils, stopping at a human approval gate before finalizing.
argument-hint: <what you want done>
---

Run the following request **the Cambium way**, not solo.

Request: $ARGUMENTS

How to run it:
- Hand off to the **Orchestrator** workflow. Decompose the goal and dispatch only the councils the
  task actually needs — Scouts (prior work / landscape), Labs (theory / methods / domain /
  statistics), **Verification** (rigor, methodology, evidence, domain + referee), Execution
  (experiments / ablation / iteration), and Support (packaging, docs).
- **Name each council as you use it** so the user can see the institute is working.
- Keep the findings ledger as you go.
- **Stop at a one-page gate card** before anything is finalized:
  *the decision · the options · the risks · your recommendation · APPROVE / REVISE / REJECT.*
  Continue only after the user answers. Never submit, publish, or finalize without approval.

If the request is missing detail you need (e.g., which file, what the deliverable is, where to save
it), ask one short clarifying question first, then proceed.
