# Cambium Institute — Python dependencies
# Install:  pip install -r requirements.txt

# Web server (FastAPI bridge)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
websockets>=12.0

# Development & testing
pytest>=7.0
pytest-asyncio>=0.21
httpx>=0.27
