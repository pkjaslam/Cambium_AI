# Tool index

This is the index the orchestrator consults before building anything new. Check here first: if a tool already does what you need, reuse it instead of writing a new script. This file is auto-generated by `tools/gen_tool_index.py` from the one-line purpose in each tool's module docstring. Do not hand-edit it; rerun the generator instead.

Tools indexed: 70

| Tool | What it does |
|---|---|
| `_patch_lessons.py` | Patch script: append one new lesson to each of three thin modules. |
| `agent_eval.py` | deterministic reliability eval for a Cambium trajectory. |
| `ai_disclosure.py` | assemble an AI-use disclosure and audit summary for a research deliverable. |
| `audit_log.py` | turn-level, append-only audit trail (review Fix #2 residual). |
| `budget_narrative_match.py` | flag mismatches between a proposal budget and its |
| `budget_review.py` | deterministic budget-to-solicitation review for research proposals. |
| `cambium_io.py` | UTF-8 stdout/stderr guard for Windows terminals, plus the |
| `cambium_run.py` | Cambium auto-runner -- turns the agent specs into running workers. |
| `cambium_start.py` | Act I "first paint" in ONE command, so /cambium can never silently fall back to plain text. |
| `check_agents.py` | Agent frontmatter validator for the Cambium. |
| `checklist_builder.py` | turn a structured solicitation rules file into a |
| `closeout.py` | advisory close-out review for the Support council (PRESENTATION Act IV). |
| `concept_graph.py` | In-house, fully-local knowledge-graph layer for Cambium. |
| `consistency_check.py` | Consistency check (v3.3) -- keeps the repo's stated numbers honest. |
| `data_scan.py` | an automated regulated/PII detector (AI_POLICY §6, POSITIONING #6). |
| `deterministic_checks.py` | verification that needs no LLM to be trusted. |
| `dispatch_plan.py` | turn route(task) into a literal, copy-ready dispatch script. |
| `doctor.py` | Cambium doctor -- repo health + self-grade (inspired by ECC/ruflo metaharness). |
| `draft_diff.py` | record what a human changed in an AI-drafted deliverable (review Fix #4 residual). |
| `enforce.py` | the single "enforce all" gauntlet for Cambium's governance controls. |
| `fair_descriptor.py` | describe a Cambium run's outputs as a FAIR Data Package. |
| `finding_audit.py` | independent check on agents' self-reported findings (eval suggestion #6). |
| `funder_freshness.py` | Cambium per-funder governance corpus freshness checker. |
| `gate.py` | the live gate interlock (eval #3) + Learning-Gate enforcement (PHILOSOPHY.md §5, repair #1). |
| `gate_lock.py` | turn the Learning Gate from a contract into a HARD runtime interlock (open thread #2). |
| `gen_3d.py` | generate a .glb 3D model from an image (or text) via a free Hugging Face Space. |
| `gen_agent_cards.py` | Generate A2A-style Agent Cards for Cambium's roster (P2). |
| `gen_board_image.py` | Render the Cambium run board as a PNG + animated GIF (no browser needed; pure PIL). |
| `gen_board_pro.py` | the premium live Cambium run board (Cowork artifact / browser). |
| `gen_brand_assets.py` | Cambium 'AI Research Institution' emblem · vector recreation of the uploaded ring+node-network logo, |
| `gen_capabilities.py` | auto-generate assets/capabilities.svg and assets/adopted-ideas.svg |
| `gen_dashboard.py` | regenerate assets/benchmark_dashboard.html from LIVE tool output. |
| `gen_demo_gif.py` | Generate assets/demo.gif from current counts (read from agent_cards.json). |
| `gen_example_gif.py` | record `cambium_run.py example` into a scrolling terminal GIF for the README demo. |
| `gen_gate_card.py` | never present a thin gate. |
| `gen_inline_board.py` | the IN-CHAT live Cambium run board (a show_widget fragment). |
| `gen_learning_lab.py` | turn a learning spec into an interactive Cambium Learning Lab page. |
| `gen_lifecycle.py` | emit assets/lifecycle.svg |
| `gen_org_chart.py` | Generate assets/org-chart.svg from agent_cards.json (so the count is never stale). |
| `gen_readme.py` | Auto-sync factual blocks in README.md between marker pairs. |
| `handoff.py` | durable Cambium memory across context windows (the /cambium:pause + /cambium:resume pair). |
| `institution_profile.py` | load, validate, and summarize a Cambium institution profile. |
| `learning_delivery.py` | enforce that a build/analysis run delivers a learning artifact. |
| `learning_gate.py` | enforce the Director's contribution at a gate (PHILOSOPHY.md §5). |
| `loop_costs.py` | Four-Cost Loop Guard for Cambium (Loop Engineering paper defense). |
| `mcp_discovery.py` | the toolsmith's live MCP awareness. |
| `memory_recall.py` | Lexical (plus optional dense) semantic recall over Cambium's |
| `model_router.py` | Cambium model router (v3.3 -- adds a reasoning tier). |
| `new_project.py` | project scaffolder (v3.1). |
| `okf_export.py` | Export a Cambium run as an Open Knowledge Format (OKF) bundle. |
| `pace_check.py` | enforce "pace as a feature" (POSITIONING #8, AI_POLICY §8) as a real control. |
| `paper_search.py` | grounded scholarly retrieval for the Scouts/Librarian (no API key required). |
| `pii_screen.py` | screen text for personal and sensitive data before it is handled or shared. |
| `proposal_timeline.py` | backwards-planned deadline and task tracker for a proposal. |
| `provenance.py` | a machine-checkable provenance manifest for Code-verified claims. |
| `roles_check.py` | validate a multi-PI roles file and look up a gate's required approver (open thread #9, Stage-1.5). |
| `rules_handoff.py` | validate a Vandalizer-to-Cambium rules handoff for budget_review. |
| `run_fidelity.py` | a per-run close-out scorecard that makes orchestrator skips visible. |
| `run_outcome_prior.py` | Heuristic prior over historical logs for Cambium runs. |
| `run_state.py` | maintain the live run-board state (agent_outputs/run_state.json). |
| `run_trace.py` | show Cambium's workflow AND its live progress, in whatever form the reader can see. |
| `solicitation_explainer.py` | render structured solicitation rules into a plain-language |
| `statusline.py` | Cambium status line -- shows live context-window heat so you pause BEFORE auto-compaction. |
| `sync_plugin_agents.py` | Mirror .claude/agents/*.md into agents/ -- the plugin's auto-discovery copy. |
| `sync_version.py` | stamp the latest CHANGELOG version into the three manifests. |
| `tamper_record.py` | emit a TaMPER record and a Four-Pillars self-check for a Cambium run. |
| `task_router.py` | Cambium Task Router -- pick the councils a task needs, build a phase plan. |
| `toolsmith.py` | Toolsmith registry -- recommend existing tools for a task (reuse beats rebuild). |
| `url_health.py` | Advisory citation-URL liveness check for Cambium (ADR-027). |
| `whoami.py` | look up a person or role in the Institute. |
